# output-styles/ — 出力スタイル

`<name>.md`(frontmatter + 指示)で Claude の応答スタイル(system prompt)を調整する。`/output-style` で切替。

frontmatter:
- `name` / `description`(UI 表示)
- `keep-coding-instructions: true`(Claude Code 既定のコーディング指示を保持したまま追記)
