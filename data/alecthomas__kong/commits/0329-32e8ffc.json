{
  "seq": 329,
  "sha": "32e8ffc6fd604e48254aff32c7d3f64d7d4db7cf",
  "parents": [
    "c62bf25854a00f09783ceead8033602712d24d84"
  ],
  "author": {
    "name": "Jan Heuermann",
    "email": "jan@jotaen.net",
    "date": "2022-10-17T14:05:22+02:00"
  },
  "message": "Fix large number processing for unsigned types",
  "pr": {
    "number": 338,
    "title": "Fix large number decoding for unsigned types in JSON",
    "body": "I noticed that after merging [the fix for large numbers (#334)](https://github.com/alecthomas/kong/pull/334), the build was broken on the `master` branch [due to a linter complaint](https://github.com/alecthomas/kong/actions/runs/3101957356/jobs/5023801376).\r\n\r\nProblem is that the `uintDecoder` is no longer a code duplication of `intDecoder`, because the adjustments by #334 only changed `intDecoder`. It’s not the linter directive that’s obsolete, but I rather think that `uintDecoder` must have that fix as well, because it suffers from the same problem.\r\n\r\nIn addition to the actual fix, I slightly adjusted the tests:\r\n\r\n- I converted the Github link to an inlined explanation, just for convenience.\r\n- Both cases for `int` and `uint` are now covered. I think it makes sense to have them together in one single test, since they are so closely related. I enclosed each one in a block, so that they have their own scope.\r\n- For the sake of completeness/consistency, I added another test for floats, which covers the `floatDecoder`.\r\n- Instead of having a helper variable `n` in the test, I think it’s more robust to inline the number. That way, we don’t rely on how `fmt.Sprintf` would serialise the number within the test.\r\n- Writing `n := 100000` would infer the type to `int`, but [`int` is platform-dependent](https://go.dev/tour/basics/11). It’s safer to assign the type explicitly (e.g. `int64`), to make the test deterministic.\r\n\r\nOne note aside: the tests don’t seem to be run on PRs. Maybe that could be fixed, which would uncover build failures before they go into the `master` branch.",
    "url": "https://github.com/alecthomas/kong/pull/338"
  },
  "linkedIssues": [],
  "stats": {
    "filesChanged": 2,
    "additions": 48,
    "deletions": 12
  },
  "files": [
    {
      "path": "mapper.go",
      "status": "M",
      "additions": 4,
      "deletions": 1,
      "binary": false
    },
    {
      "path": "mapper_test.go",
      "status": "M",
      "additions": 44,
      "deletions": 11,
      "binary": false
    }
  ],
  "diff": "diff --git a/mapper.go b/mapper.go\nindex 6d36b15..65f9814 100644\n--- a/mapper.go\n+++ b/mapper.go\n@@ -392,9 +392,12 @@ func uintDecoder(bits int) MapperFunc { // nolint: dupl\n \t\tcase string:\n \t\t\tsv = v\n \n-\t\tcase int, int8, int16, int32, int64, uint, uint8, uint16, uint32, uint64, float32, float64:\n+\t\tcase int, int8, int16, int32, int64, uint, uint8, uint16, uint32, uint64:\n \t\t\tsv = fmt.Sprintf(\"%v\", v)\n \n+\t\tcase float32, float64:\n+\t\t\tsv = fmt.Sprintf(\"%0.f\", v)\n+\n \t\tdefault:\n \t\t\treturn fmt.Errorf(\"expected an int but got %q (%T)\", t, t.Value)\n \t\t}\ndiff --git a/mapper_test.go b/mapper_test.go\nindex 804ac39..e86fed7 100644\n--- a/mapper_test.go\n+++ b/mapper_test.go\n@@ -401,18 +401,51 @@ func TestNumbers(t *testing.T) {\n }\n \n func TestJSONLargeNumber(t *testing.T) {\n-\t// https://github.com/alecthomas/kong/pull/334\n-\tconst n = 1000000\n-\tvar cli struct {\n-\t\tN int64\n+\t// Make sure that large numbers are not internally converted to\n+\t// scientific notation when the mapper parses the values.\n+\t// (Scientific notation is e.g. `1e+06` instead of `1000000`.)\n+\n+\t// Large signed integers:\n+\t{\n+\t\tvar cli struct {\n+\t\t\tN int64\n+\t\t}\n+\t\tjson := `{\"n\": 1000000}`\n+\t\tr, err := kong.JSON(strings.NewReader(json))\n+\t\tassert.NoError(t, err)\n+\t\tparser := mustNew(t, &cli, kong.Resolvers(r))\n+\t\t_, err = parser.Parse([]string{})\n+\t\tassert.NoError(t, err)\n+\t\tassert.Equal(t, int64(1000000), cli.N)\n+\t}\n+\n+\t// Large unsigned integers:\n+\t{\n+\t\tvar cli struct {\n+\t\t\tN uint64\n+\t\t}\n+\t\tjson := `{\"n\": 1000000}`\n+\t\tr, err := kong.JSON(strings.NewReader(json))\n+\t\tassert.NoError(t, err)\n+\t\tparser := mustNew(t, &cli, kong.Resolvers(r))\n+\t\t_, err = parser.Parse([]string{})\n+\t\tassert.NoError(t, err)\n+\t\tassert.Equal(t, uint64(1000000), cli.N)\n+\t}\n+\n+\t// Large floats:\n+\t{\n+\t\tvar cli struct {\n+\t\t\tN float64\n+\t\t}\n+\t\tjson := `{\"n\": 1000000.1}`\n+\t\tr, err := kong.JSON(strings.NewReader(json))\n+\t\tassert.NoError(t, err)\n+\t\tparser := mustNew(t, &cli, kong.Resolvers(r))\n+\t\t_, err = parser.Parse([]string{})\n+\t\tassert.NoError(t, err)\n+\t\tassert.Equal(t, float64(1000000.1), cli.N)\n \t}\n-\tjson := fmt.Sprintf(`{\"n\": %d}`, n)\n-\tr, err := kong.JSON(strings.NewReader(json))\n-\tassert.NoError(t, err)\n-\tparser := mustNew(t, &cli, kong.Resolvers(r))\n-\t_, err = parser.Parse([]string{})\n-\tassert.NoError(t, err)\n-\tassert.Equal(t, n, cli.N)\n }\n \n func TestFileMapper(t *testing.T) {\n",
  "diffTruncated": false,
  "tree": [
    ".github/FUNDING.yml",
    ".github/workflows/ci.yml",
    ".gitignore",
    ".golangci.yml",
    "COPYING",
    "README.md",
    "_examples/docker/README.md",
    "_examples/docker/commands.go",
    "_examples/docker/main.go",
    "_examples/server/README.md",
    "_examples/server/console.go",
    "_examples/server/go.mod",
    "_examples/server/go.sum",
    "_examples/server/main.go",
    "_examples/server/server_rsa_key",
    "_examples/server/server_rsa_key.pub",
    "_examples/shell/commandstring/main.go",
    "_examples/shell/help/README.md",
    "_examples/shell/help/main.go",
    "benchmark_test.go",
    "bin/.go-1.19.1.pkg",
    "bin/.golangci-lint-1.46.2.pkg",
    "bin/README.hermit.md",
    "bin/activate-hermit",
    "bin/go",
    "bin/gofmt",
    "bin/golangci-lint",
    "bin/hermit",
    "bin/hermit.hcl",
    "build.go",
    "callbacks.go",
    "camelcase.go",
    "config_test.go",
    "context.go",
    "defaults.go",
    "defaults_test.go",
    "doc.go",
    "error.go",
    "global.go",
    "global_test.go",
    "go.mod",
    "go.sum",
    "guesswidth.go",
    "guesswidth_unix.go",
    "help.go",
    "help_test.go",
    "helpwrap1.18_test.go",
    "helpwrap1.19_test.go",
    "hooks.go",
    "interpolate.go",
    "interpolate_test.go",
    "kong.go",
    "kong.png",
    "kong.sketch",
    "kong_test.go",
    "levenshtein.go",
    "mapper.go",
    "mapper_linux_test.go",
    "mapper_test.go",
    "mapper_windows_test.go",
    "model.go",
    "model_test.go",
    "options.go",
    "options_test.go",
    "resolver.go",
    "resolver_test.go",
    "scanner.go",
    "scanner_test.go",
    "tag.go",
    "tag_test.go",
    "testdata/file.txt",
    "util.go",
    "util_test.go",
    "visit.go"
  ],
  "explanation": {
    "what": "前コミット(#334)で intDecoder にのみ適用された float32/float64 の E記法回避フォーマット(`%0.f`)を、uintDecoder にも同様に適用した。TestJSONLargeNumber を、符号付き整数・符号なし整数・float の3ケースをそれぞれ独立したブロックにまとめる形に書き直し、uint64 と float64 の大きな数値についても検証を追加している。",
    "why": "PR本文によれば、#334 は intDecoder のみを修正し uintDecoder には同じ修正を入れなかったため、uintDecoder は intDecoder との「意図的なコード重複」を示す `// nolint: dupl` コメントの前提が崩れ、master ブランチの lint が壊れる結果になっていた。uintDecoder は intDecoder と同じ問題(float64 由来の大きな数値が E記法になる)を抱えていたため、同じ修正を適用して重複を回復し、テストも int/uint/float の3系統を明示的に分けて意図を明確にした。",
    "highlights": [
      { "file": "mapper.go", "note": "uintDecoder にも `%0.f` フォーマットを適用し、intDecoder との実装重複(nolint: dupl の前提)を回復" },
      { "file": "mapper_test.go", "note": "TestJSONLargeNumber を int64/uint64/float64 の3ブロックに分割し、それぞれ独立した scope でテスト" }
    ],
    "langNotes": [],
    "diagram": null,
    "evidence": "pr",
    "refs": [{ "type": "pr", "number": 338, "url": "https://github.com/alecthomas/kong/pull/338" }],
    "model": "claude-sonnet-5",
    "generatedAt": "2026-07-22T00:00:00Z"
  }
}
